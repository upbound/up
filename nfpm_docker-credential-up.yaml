name: up
arch: "${GOARCH}"
platform: "${GOOS}"
version: "${VERSION}"
section: "default"
priority: "extra"
maintainer: "Dan Mangum <dan@upbound.io>"
description: Upbound Docker credential helper.
homepage: "https://upbound.io"
license: "Upbound Software License"
contents:
- src: "${GO_BIN_DIR}/${PLATFORM}/docker-credential-up"
  dst: /usr/local/bin/docker-credential-up
